{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "run",
			"type": "shell",
			"command": "cargo run -p $(dirname ${relativeFileDirname}) --release",
			"group": {
                "kind": "build",
                "isDefault": true
            }
		}
	]
}